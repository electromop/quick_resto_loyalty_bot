import asyncio

from utils.quick_resto import create_client, make_token_for_client, get_bonus_history

asyncio.run(create_client("5", "6", "79151913887"))

# asyncio.run(make_token_for_client(7, "+79151913886"))

# asyncio.run(get_bonus_history(7))

